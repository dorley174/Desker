apiVersion: v1
kind: Service
metadata:
  name: {{ include "desker-backend.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "desker-backend.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
