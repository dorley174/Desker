apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: desker-sqlite-pvc
  namespace: team-6-ns
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: manual
  volumeName: desker-sqlite-pv